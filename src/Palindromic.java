import java.util.HashMap;

/**
 * Created by Chenggang on 17/12/14.
 */
/*
public class Palindromic {
    public String LongestPalondromic(String s){
        HashMap<Integer,Character> map = new HashMap<Integer,Character>();
        for(int i = s.length()-1;i>=0;i++){
            map.put(s.length()-i-1,s.charAt(i));
        }
        s.su
    }
    public HashMap<Integer,String> LongestCommonString(HashMap<Integer,String> map1,HashMap<Integer,String> map2){
        for(int i = 0;i<map1.size();i++){
            if(map2.containsValue(map1.get(i)){

            }
        }
    }
}
*/